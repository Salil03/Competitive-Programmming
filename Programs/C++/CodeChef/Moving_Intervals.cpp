#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int c,n;
        bool k;
        cin >> c >> n >> k;
        vector<pair<int, int>> times;
        int x,y;
        for(int i = 0; i<n ; i++)
        {
            cin >> x >> y;
            times.push_back(make_pair(x,y));
        }
        if(k == 0)
        {

        }
    }
}
