#include<bits/stdc++.h>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	int t;
	cin >> t;
	while(t--)
	{
		unsigned long long int n,temp;
		cin >> n;
		cout << n*(n-1)/2 << "\n";
		while(n--)
		{
			cin >> temp;
		}
	}
}