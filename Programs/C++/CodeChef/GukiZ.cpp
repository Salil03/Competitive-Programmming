#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t = 0, n = 0, z1 = 0, z2 = 0;
    cin >> t;
    while (t--)
    {
        cin >> n >> z1 >> z2;
        int arr[n];
        for (int i = 0; i < n; i++)
        {
            cin >> arr[n];
        }
        for (int i = 0; i < 10000000000; i++)
        {
            if (z1 == 0 || z2 == 0)
            {
                cout << 1;
            }
            else if (z1 < 0 || z2 = < 0)
            {
                cout << 2;
            }
            else
            {
                z
            }
        }
    }
}