#include<bits/stdc++.h>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	int n,w;
	cin >> n >> w;
	int weight[n];
	int value[n];
	for(int i = 0; i<n; i++)
	{
		cin >> weight[i] >> value[i];
	}
}